// Package logrotation provides a writer that automatically rotates logfiles
// if the set maximum size is reached or exceeded. Compression is enabled by
// default, but can be disabled if required.
package logrotation
